- handle text substitution (eg @@PERL_VERSION@@)

- support release operation:
    - removes CVS/ dirs
    - does .tar.gz
  
- handle -co across cvs repositories

- write User Guide

- add ant 1.5 note to setup doc

- bug: glitch in the chmod +x of bin/
