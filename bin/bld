#!/bin/bash

if [ -z "$1" ]; then
   echo ""
   echo "Usage: bld project[/component]"
   echo ""
   echo "  calls 'build project[/component] install -append'"
   echo ""
   exit 1
fi
build $1 install -append

